// @ts-nocheck
import {createRoot} from 'react-dom/client';
import {FormEvent,ReactNode,createContext,useContext,useEffect,useState} from 'react';
import {BrowserRouter,Link,Navigate,Route,Routes,useNavigate,useParams} from 'react-router-dom';
import {z} from 'zod'; import './styles.css';
type Role='SYSTEM_OWNER'|'BUSINESS_ADMIN'; type User={id:string;name:string;email:string;role:Role;businessId:string|null}; type Business={id:string;name:string;contactEmail:string;contactPhone:string;timezone:string;status:'ACTIVE'|'DISABLED'}; type Service={id:string;name:string;description:string|null;durationMinutes:number;status:'ACTIVE'|'INACTIVE'}; type Staff={id:string;name:string;email:string;status:'ACTIVE'|'DISABLED'}; type Availability={id:string;staffId:string|null;dayOfWeek:number;startTime:string;endTime:string;status:'ACTIVE'|'DISABLED'}; type Appointment={id:string;bookingReference:string;customerName:string;customerEmail:string;customerPhone:string;startAt:string;endAt:string;status:'CONFIRMED'|'COMPLETED'|'CANCELLED'|'NO_SHOW';service:Service;staff:Staff|null};
class ApiError extends Error{constructor(public status:number,msg:string){super(msg)}}; async function call<T>(url:string,init:RequestInit={}):Promise<T>{const r=await fetch(url,{credentials:'include',headers:{'Content-Type':'application/json'},...init});const b=await r.json().catch(()=>({}));if(!r.ok||b.success===false)throw new ApiError(r.status,b.error?.message||'Request failed');return b as T} function post<T>(url:string,body:unknown):Promise<T>{return call<T>(url,{method:'POST',body:JSON.stringify(body)})} function patch<T>(url:string,body:unknown):Promise<T>{return call<T>(url,{method:'PATCH',body:JSON.stringify(body)})}
const api={login:(email:string,password:string)=>post<{user:User}>('/api/auth/login',{email,password}),me:()=>call<{user:User}>('/api/auth/me'),logout:()=>post('/api/auth/logout',{}),businesses:()=>call<{businesses:Business[]}>('/api/businesses'),createBusiness:(x:unknown)=>post('/api/businesses',x),businessStatus:(id:string,status:string)=>patch(`/api/businesses/${id}/status`,{status}),profile:()=>call<{business:Business}>('/api/business/profile'),updateProfile:(x:unknown)=>patch('/api/business/profile',x),services:()=>call<{services:Service[]}>('/api/services'),staff:()=>call<{staff:Staff[]}>('/api/staff'),availability:()=>call<{availability:Availability[]}>('/api/availability'),appointments:()=>call<{appointments:Appointment[]}>('/api/appointments'),createService:(x:unknown)=>post('/api/services',x),createStaff:(x:unknown)=>post('/api/staff',x),createAvailability:(x:unknown)=>post('/api/availability',x),delete:(x:string)=>call(x,{method:'DELETE'}),bookingBusiness:(id:string)=>call<{business:Business}>(`/api/public/businesses/${id}`),bookingServices:(id:string)=>call<{services:Service[]}>(`/api/public/businesses/${id}/services`),bookingStaff:(id:string)=>call<{staff:Staff[]}>(`/api/public/businesses/${id}/staff`),slots:(b:string,s:string,date:string,staff?:string)=>call<{slots:string[]}>(`/api/public/businesses/${b}/services/${s}/slots?date=${date}${staff?`&staffId=${staff}`:''}`),book:(x:unknown)=>post<{appointment:Appointment}>('/api/appointments',x),lookup:(reference:string,email:string)=>call<{appointment:Appointment & {business:Business}}>(`/api/public/bookings?reference=${encodeURIComponent(reference)}&email=${encodeURIComponent(email)}`),cancel:(reference:string,email:string)=>post('/api/public/bookings/cancel',{reference,email})};
const Auth=createContext<{user:User|null;ready:boolean;setUser:(x:User|null)=>void}>({user:null,ready:false,setUser:()=>{}});const useAuth=()=>useContext(Auth); function Provider({children}:{children:ReactNode}){const [user,setUser]=useState<User|null>(null),[ready,setReady]=useState(false);useEffect(()=>{api.me().then(x=>setUser(x.user)).catch(()=>{}).finally(()=>setReady(true))},[]);return <Auth.Provider value={{user,ready,setUser}}>{children}</Auth.Provider>};const message=(e:unknown)=>e instanceof Error?e.message:'Something went wrong.';
function Guard({children,role}:{children:ReactNode;role?:Role}){const a=useAuth();if(!a.ready)return <p>Loading…</p>;if(!a.user)return <Navigate to="/login"/>;if(role&&a.user.role!==role)return <Navigate to="/dashboard"/>;return <>{children}</>}; function Login(){const a=useAuth(),n=useNavigate(),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(a.user)return <Navigate to="/dashboard"/>;async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));const v=z.object({email:z.string().email('Enter a valid email.'),password:z.string().min(8,'Password must be at least 8 characters.')}).safeParse(d);if(!v.success){setError(v.error.issues[0].message);return}setBusy(true);try{const r=await api.login(v.data.email,v.data.password);a.setUser(r.user);n('/dashboard')}catch(e){setError(message(e))}finally{setBusy(false)}}return <main className="login"><section><small>HANABI TECHNOLOGIES</small><h1>Sign in</h1><p>Manage your appointment business.</p>{error&&<p className="alert">{error}</p>}<form onSubmit={submit}><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label><button disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form></section></main>}
function Shell({children}:{children:ReactNode}){const a=useAuth(),n=useNavigate();const links=a.user?.role==='SYSTEM_OWNER'?[['Overview','/dashboard'],['Businesses','/admin']]:[['Dashboard','/dashboard'],['Business','/business'],['Services','/services'],['Staff','/staff'],['Availability','/availability'],['Appointments','/appointments']];return <div className="shell"><aside><Link className="brand" to="/dashboard">hanabi<span>booking</span></Link><nav>{links.map(([t,u])=><Link to={u} key={u}>{t}</Link>)}</nav><small>{a.user?.email}</small><button className="text" onClick={()=>api.logout().finally(()=>{a.setUser(null);n('/login')})}>Log out</button></aside><main>{children}</main></div>};function Page({title,children,action}:{title:string;children:ReactNode;action?:ReactNode}){return <><header><div><h1>{title}</h1></div>{action}</header>{children}</>};const Badge=({x}:{x:string})=><span className={`badge ${x==='ACTIVE'||x==='CONFIRMED'?'ok':''}`}>{x}</span>;
function Dashboard(){const a=useAuth();return a.user?.role==='SYSTEM_OWNER'?<Businesses/>:<AdminDashboard/>} function AdminDashboard(){const [stats,setStats]=useState<number[]|null>(null);useEffect(()=>{Promise.all([api.services(),api.staff(),api.appointments()]).then(([s,t,a])=>setStats([s.services.filter(x=>x.status==='ACTIVE').length,t.staff.length,a.appointments.filter(x=>new Date(x.startAt)>new Date()).length,a.appointments.filter(x=>new Date(x.startAt).toDateString()===new Date().toDateString()).length]))},[]);return <Page title="Dashboard">{!stats?<p>Loading dashboard…</p>:<div className="stats">{['Active services','Team members','Upcoming','Today'].map((x,i)=><article key={x}><strong>{stats[i]}</strong><span>{x}</span></article>)}</div>}</Page>}
function Businesses(){const [items,setItems]=useState<Business[]|null>(null),[show,setShow]=useState(false),[error,setError]=useState('');const load=()=>api.businesses().then(x=>setItems(x.businesses)).catch(e=>setError(message(e)));useEffect(()=>{void load()},[]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();try{await api.createBusiness(Object.fromEntries(new FormData(e.currentTarget)));setShow(false);void load()}catch(e){setError(message(e))}}if(!items)return <p>Loading businesses…</p>;return <Page title="Businesses" action={<button onClick={()=>setShow(true)}>New business</button>}>{error&&<p className="alert">{error}</p>}{show&&<div className="card"><form onSubmit={save} className="grid"><label>Name<input name="name" required/></label><label>Email<input name="contactEmail" type="email" required/></label><label>Phone<input name="contactPhone" required/></label><label>Timezone<input name="timezone" defaultValue="Asia/Kolkata" required/></label><label>Status<select name="status"><option>ACTIVE</option><option>DISABLED</option></select></label><button>Create</button></form></div>}<table><thead><tr><th>Name</th><th>Contact</th><th>Timezone</th><th>Status</th><th/></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.contactEmail}<br/>{x.contactPhone}</td><td>{x.timezone}</td><td><Badge x={x.status}/></td><td><button className="secondary" onClick={()=>api.businessStatus(x.id,x.status==='ACTIVE'?'DISABLED':'ACTIVE').then(()=>load())}>{x.status==='ACTIVE'?'Disable':'Activate'}</button></td></tr>)}</tbody></table></Page>}
function Profile(){const [b,setB]=useState<Business|null>(null),[note,setNote]=useState('');useEffect(()=>{api.profile().then(x=>setB(x.business)).catch(e=>setNote(message(e)))},[]);if(!b)return <p>Loading profile…</p>;return <Page title="Business profile"><p className="oktext">{note}</p><form className="card grid" onSubmit={async e=>{e.preventDefault();try{const r=await api.updateProfile(Object.fromEntries(new FormData(e.currentTarget)));setB(r.business);setNote('Profile saved.')}catch(e){setNote(message(e))}}}><label>Name<input name="name" defaultValue={b.name} required/></label><label>Email<input name="contactEmail" type="email" defaultValue={b.contactEmail} required/></label><label>Phone<input name="contactPhone" defaultValue={b.contactPhone} required/></label><label>Timezone<input name="timezone" defaultValue={b.timezone} required/></label><button>Save changes</button></form></Page>}
function Crud({kind}:{kind:'services'|'staff'}){const isService=kind==='services';const [items,setItems]=useState<(Service|Staff)[]|null>(null),[show,setShow]=useState(false),[note,setNote]=useState('');const load=()=> (isService?api.services():api.staff()).then(x=>setItems((isService?(x as {services:Service[]}).services:(x as {staff:Staff[]}).staff))).catch(e=>setNote(message(e)));useEffect(()=>{void load()},[]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));if(isService&&Number(d.durationMinutes)<=0){setNote('Duration must be a positive number.');return}try{isService?await api.createService({...d,durationMinutes:Number(d.durationMinutes)}):await api.createStaff(d);setShow(false);void load()}catch(e){setNote(message(e))}}if(!items)return <p>Loading {kind}…</p>;return <Page title={isService?'Services':'Staff'} action={<button onClick={()=>setShow(true)}>Add {isService?'service':'staff'}</button>}><p className="alert">{note}</p>{show&&<form className="card grid" onSubmit={save}><label>Name<input name="name" required/></label>{isService?<><label>Description<input name="description"/></label><label>Duration<input name="durationMinutes" type="number" min="1" required/></label><label>Status<select name="status"><option>ACTIVE</option><option>INACTIVE</option></select></label></>:<><label>Email<input name="email" type="email" required/></label><label>Status<select name="status"><option>ACTIVE</option><option>DISABLED</option></select></label></>}<button>Create</button></form>}<div className="rows">{items.length===0?<p>No {kind} have been configured yet.</p>:items.map(x=><article className="row" key={x.id}><div><strong>{x.name}</strong><br/><small>{isService?`${(x as Service).description||'No description'} · ${(x as Service).durationMinutes} minutes`:(x as Staff).email}</small></div><Badge x={x.status}/><button className="text" onClick={()=>{if(confirm('Delete this item?'))api.delete(`/api/${kind}/${x.id}`).then(()=>load())}}>Delete</button></article>)}</div></Page>}
function AvailabilityPage(){const [items,setItems]=useState<Availability[]|null>(null),[staff,setStaff]=useState<Staff[]>([]),[note,setNote]=useState('');const load=()=>Promise.all([api.availability(),api.staff()]).then(([a,s])=>{setItems(a.availability);setStaff(s.staff)}).catch(e=>setNote(message(e)));useEffect(load,[]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget)) as Record<string,string>;if(d.startTime>=d.endTime){setNote('Start time must be before end time.');return}try{await api.createAvailability({dayOfWeek:Number(d.dayOfWeek),startTime:d.startTime,endTime:d.endTime,staffId:d.staffId||null,status:d.status});load();e.currentTarget.reset()}catch(e){setNote(message(e))}}const days=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];if(!items)return <p>Loading availability…</p>;return <Page title="Availability"><p className="alert">{note}</p><form className="card grid" onSubmit={save}><label>Day<select name="dayOfWeek">{days.map((x,i)=><option value={i} key={x}>{x}</option>)}</select></label><label>Start<input name="startTime" type="time" required/></label><label>End<input name="endTime" type="time" required/></label><label>Staff<select name="staffId"><option value="">Business-wide</option>{staff.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Status<select name="status"><option>ACTIVE</option><option>DISABLED</option></select></label><button>Add availability</button></form><div className="rows">{items.length===0?<p>No availability has been configured yet.</p>:items.map(x=><article className="row" key={x.id}><div><strong>{days[x.dayOfWeek]} {x.startTime}–{x.endTime}</strong><br/><small>{staff.find(s=>s.id===x.staffId)?.name||'Business-wide'}</small></div><Badge x={x.status}/><button className="text" onClick={()=>api.delete(`/api/availability/${x.id}`).then(load)}>Remove</button></article>)}</div></Page>}
function Appointments(){const [all,setAll]=useState<Appointment[]|null>(null),[status,setStatus]=useState('');useEffect(()=>{api.appointments().then(x=>setAll(x.appointments))},[]);if(!all)return <p>Loading appointments…</p>;const items=all.filter(x=>!status||x.status===status);return <Page title="Appointments"><label>Filter by status<select value={status} onChange={e=>setStatus(e.target.value)}><option value="">All</option>{['CONFIRMED','COMPLETED','CANCELLED','NO_SHOW'].map(x=><option key={x}>{x}</option>)}</select></label><div className="rows">{items.length===0?<p>No appointments found.</p>:items.map(x=><article className="row" key={x.id}><div><strong>{x.customerName} · {x.bookingReference}</strong><br/><small>{x.service.name} · {new Date(x.startAt).toLocaleString()} · {x.customerEmail}</small></div><Badge x={x.status}/><select value={x.status} onChange={e=>{if(e.target.value==='CANCELLED'&&!confirm('Cancel this appointment?'))return;patch(`/api/appointments/${x.id}/status`,{status:e.target.value}).then(()=>api.appointments()).then(r=>setAll(r.appointments))}}>{['CONFIRMED','COMPLETED','CANCELLED','NO_SHOW'].map(s=><option key={s}>{s}</option>)}</select></article>)}</div></Page>}
function Booking(){const {businessId=''}=useParams(),nav=useNavigate();const [b,setB]=useState<Business|null>(null),[services,setServices]=useState<Service[]>([]),[staff,setStaff]=useState<Staff[]>([]),[service,setService]=useState<Service|null>(null),[staffId,setStaffId]=useState(''),[date,setDate]=useState(''),[slots,setSlots]=useState<string[]>([]),[slot,setSlot]=useState(''),[details,setDetails]=useState({customerName:'',customerEmail:'',customerPhone:''}),[step,setStep]=useState(1),[note,setNote]=useState('');useEffect(()=>{Promise.all([api.bookingBusiness(businessId),api.bookingServices(businessId),api.bookingStaff(businessId)]).then(([x,y,z])=>{setB(x.business);setServices(y.services);setStaff(z.staff)}).catch(e=>setNote(message(e)))},[businessId]);useEffect(()=>{if(service&&date)api.slots(businessId,service.id,date,staffId||undefined).then(x=>setSlots(x.slots)).catch(e=>setNote(message(e)))} ,[service,date,staffId,businessId]);if(note&&!b)return <main className="booking"><h1>Booking unavailable</h1><p>{note}</p></main>;if(!b)return <main className="booking">Loading booking experience…</main>;const validDetails=z.object({customerName:z.string().min(1,'Name is required.'),customerEmail:z.string().email('Valid email required.'),customerPhone:z.string().min(7,'Valid phone required.')}).safeParse(details);async function confirm(){if(!service||!slot)return;try{const r=await api.book({businessId,serviceId:service.id,staffId:staffId||null,startAt:slot,...details,timezone:b.timezone});nav(`/booking/confirmation/${r.appointment.bookingReference}`,{state:{appointment:r.appointment,business:b}})}catch(e){if(e instanceof ApiError&&e.status===409){setNote('This time slot is no longer available. Please select another time.');setStep(3);api.slots(businessId,service.id,date,staffId||undefined).then(x=>setSlots(x.slots))}else setNote(message(e))}}return <main className="booking"><Link to="/booking">Look up an existing booking</Link><small>{b.timezone}</small><h1>Book with {b.name}</h1>{note&&<p className="alert">{note}</p>}<div className="steps">Step {step} of 5</div>{step===1&&<section><h2>Select a service</h2><div className="choices">{services.map(x=><button className={service?.id===x.id?'chosen':''} key={x.id} onClick={()=>{setService(x);setStep(2)}}><strong>{x.name}</strong><br/><small>{x.description} · {x.durationMinutes} min</small></button>)}</div></section>}{step===2&&<section><h2>Select date and staff</h2><label>Date<input type="date" min={new Date().toISOString().slice(0,10)} value={date} onChange={e=>setDate(e.target.value)}/></label><label>Staff<select value={staffId} onChange={e=>setStaffId(e.target.value)}><option value="">Business-wide availability</option>{staff.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><button disabled={!date} onClick={()=>setStep(3)}>View available times</button></section>}{step===3&&<section><h2>Available times</h2>{slots.length===0?<p>No available slots for this date.</p>:<div className="slots">{slots.map(x=><button key={x} className={slot===x?'chosen':''} onClick={()=>{setSlot(x);setStep(4)}}>{new Intl.DateTimeFormat(undefined,{hour:'numeric',minute:'2-digit',timeZone:b.timezone}).format(new Date(x))}</button>)}</div>}</section>}{step===4&&<section><h2>Your details</h2><form className="grid" onSubmit={e=>{e.preventDefault();if(!validDetails.success){setNote(validDetails.error.issues[0].message);return}setNote('');setStep(5)}}><label>Name<input value={details.customerName} onChange={e=>setDetails({...details,customerName:e.target.value})}/></label><label>Email<input type="email" value={details.customerEmail} onChange={e=>setDetails({...details,customerEmail:e.target.value})}/></label><label>Phone<input value={details.customerPhone} onChange={e=>setDetails({...details,customerPhone:e.target.value})}/></label><button>Review booking</button></form></section>}{step===5&&<section className="card"><h2>Review booking</h2><p>{b.name} · {service?.name} · {service?.durationMinutes} minutes</p><p>{new Intl.DateTimeFormat(undefined,{dateStyle:'full',timeStyle:'short',timeZone:b.timezone}).format(new Date(slot))} ({b.timezone})</p><p>{details.customerName}<br/>{details.customerEmail}<br/>{details.customerPhone}</p><button onClick={confirm}>Confirm booking</button><button className="text" onClick={()=>setStep(4)}>Back</button></section>}</main>}
function Confirmation(){const {bookingReference=''}=useParams(),loc=history.state?.usr as {appointment?:Appointment,business?:Business}|undefined;const a=loc?.appointment;return <main className="booking"><h1>Booking confirmed</h1><p>Keep this reference: <strong>{bookingReference}</strong></p>{a&&<><p>{a.service.name} · {new Date(a.startAt).toLocaleString()}</p><p>Status: <Badge x={a.status}/></p></>}<Link to="/booking">Done</Link></main>}
function Lookup(){const [a,setA]=useState<(Appointment & {business:Business})|null>(null),[note,setNote]=useState(''),[values,setValues]=useState({reference:'',email:''});async function find(e:FormEvent){e.preventDefault();try{setA((await api.lookup(values.reference,values.email)).appointment);setNote('')}catch(e){setA(null);setNote(message(e))}}return <main className="booking"><h1>Find your booking</h1><p>{note}</p><form className="grid" onSubmit={find}><label>Booking reference<input value={values.reference} onChange={e=>setValues({...values,reference:e.target.value})} required/></label><label>Email<input type="email" value={values.email} onChange={e=>setValues({...values,email:e.target.value})} required/></label><button>Find booking</button></form>{a&&<article className="card"><h2>{a.business.name}</h2><p>{a.service.name} · {new Date(a.startAt).toLocaleString()}<br/>Reference: {a.bookingReference}<br/><Badge x={a.status}/></p>{a.status!=='CANCELLED'&&<button className="danger" onClick={()=>{if(confirm('Cancel this booking?'))api.cancel(a.bookingReference,values.email).then(()=>{setA({...a,status:'CANCELLED'});setNote('Booking cancelled.')}).catch(e=>setNote(message(e)))}}>Cancel booking</button>}</article>}</main>}
function App(){return <Provider><Routes><Route path="/login" element={<Login/>}/><Route path="/book/:businessId" element={<Booking/>}/><Route path="/booking" element={<Lookup/>}/><Route path="/booking/confirmation/:bookingReference" element={<Confirmation/>}/><Route path="/dashboard" element={<Guard><Shell><Dashboard/></Shell></Guard>}/><Route path="/admin" element={<Guard role="SYSTEM_OWNER"><Shell><Businesses/></Shell></Guard>}/><Route path="/business" element={<Guard role="BUSINESS_ADMIN"><Shell><Profile/></Shell></Guard>}/><Route path="/services" element={<Guard role="BUSINESS_ADMIN"><Shell><Crud kind="services"/></Shell></Guard>}/><Route path="/staff" element={<Guard role="BUSINESS_ADMIN"><Shell><Crud kind="staff"/></Shell></Guard>}/><Route path="/availability" element={<Guard role="BUSINESS_ADMIN"><Shell><AvailabilityPage/></Shell></Guard>}/><Route path="/appointments" element={<Guard role="BUSINESS_ADMIN"><Shell><Appointments/></Shell></Guard>}/><Route path="*" element={<Navigate to="/dashboard"/>}/></Routes></Provider>};createRoot(document.getElementById('root')!).render(<BrowserRouter><App/></BrowserRouter>);
